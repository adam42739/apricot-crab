{
  "name": "@apricot-crab/infra-s3",
  "version": "0.1.0",
  "description": "Reusable AWS CDK construct for a locked-down, private S3 bucket with opt-in least-privilege (bucket-policy) grants to specific IAM roles.",
  "license": "UNLICENSED",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json -w",
    "clean": "rm -rf dist",
    "prepare": "npm run build"
  },
  "peerDependencies": {
    "aws-cdk-lib": "^2.261.0",
    "constructs": "^10.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "aws-cdk-lib": "2.261.0",
    "constructs": "^10.6.0",
    "typescript": "^5.7.0"
  }
}
